# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CompareSwiftJson' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CompareSwiftJson
  pod 'ObjectMapper'
  pod 'SwiftyJSON'
  pod 'HandyJSON'
  pod 'Argo',git:'https://github.com/thoughtbot/Argo.git',branch: 'master'
  pod 'Gloss'
  pod 'Curry'
  
  target 'CompareSwiftJsonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CompareSwiftJsonUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
